Drop these files here with the exact names below and they'll be picked up automatically:

- logo.png              -> circular Lobster Factory logo (header + branding)
- hero-poster.jpg        -> fallback image shown behind/before the hero video loads
- videos/hero.mp4        -> full-screen looping hero video (boil pour, steam, grill shots)
- videos/hero.webm       -> optional smaller/faster web format of the same video
- og-image.png (1200x630) -> social share preview image (Open Graph / Twitter)

Until these exist, the hero falls back to an animated ember/steam background gradient and the
header logo is simply hidden — nothing breaks.
