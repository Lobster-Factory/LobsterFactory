Drop your image files here with these exact names and they'll appear automatically on the site:

- logo.png          -> the circular Lobster Factory logo (used in the header and hero watermark)
- menu-flyer.png     -> the boil tray / hero photo (used in the Gallery section)

The site already has graceful fallbacks if these files are missing, but it will look much better with the real artwork in place.
